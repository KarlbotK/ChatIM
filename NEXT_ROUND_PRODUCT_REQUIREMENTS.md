# ChatIM 下一轮产品与联调需求文档

## 1. 文档定位

- 本文是 `FRONTEND_PRODUCT_REQUIREMENTS.md` 的下一轮增量需求，不替代原文档。
- 当前前端已经完成登录、基础会话、文本与图片消息、好友申请、基础建群、WebSocket 重连、离线补拉和历史消息入口。
- 下一轮重点不是继续堆页面，而是把消息可靠性、跨设备会话恢复、群资料和安全存储补成可联调、可验收的完整链路。
- 本轮按三个里程碑实施。必须先完成协议和数据闭环，再开发依赖这些协议的重试、未读和群管理页面。

> 实施进度：里程碑 A 第 1—3 项已经落地。移动端使用 Bearer Header，浏览器使用一次性短期 ticket；消息链路已增加 accepted、persisted、failed ACK，以“已认证发送者 + clientMessageId”幂等，并提供发送结果查询；离线同步使用账号绑定的服务端签名游标和 MySQL 完整回源。

## 2. 本轮目标

### 2.1 P0 目标

1. 明确移动端和浏览器的 WebSocket 鉴权方式。
2. 增加可靠的消息 ACK 和 `clientMessageId` 幂等。
3. 把离线补拉从客户端时间戳升级为服务端同步游标。
4. 增加会话摘要、会话详情、已读位置和未读数接口。
5. 确保登录、断线重连、消息发送、离线恢复和 App 重启能够形成闭环。
6. 将正式移动端的令牌迁移到系统安全存储。

### 2.2 P1 目标

1. 补齐群详情、群成员和基础群管理。
2. 规范图片对象的长期保存与临时访问方式。
3. 增加消息结果查询和用户主动重试。
4. 完成红包发送、领取和详情主流程。
5. 增加会话置顶、免打扰、隐藏和好友备注。

### 2.3 本轮非目标

- 语音和视频通话。
- 大文件传输。
- 消息表情回应。
- 群投票。
- 完整的多设备同时在线。
- 桌面端和 Web 端正式发布。
- 复杂推荐流和内容社区。

## 3. 实施顺序

### 里程碑 A：可靠消息基础

1. WebSocket 鉴权方案定稿。
2. 发送 ACK、持久化 ACK 和消息幂等。
3. 服务端离线游标和完整回源。
4. 会话摘要、已读位置和未读数。
5. 真机双账号联调。

### 里程碑 B：会话和群资料完整化

1. 会话详情。
2. 群详情和成员分页。
3. 修改群名称、公告和头像。
4. 退出群聊、移除成员和解散群聊。
5. 会话置顶、免打扰和隐藏。

### 里程碑 C：产品功能完善

1. 消息失败重试和结果查询。
2. 红包发送、领取和详情。
3. 好友备注和基础联系人管理。
4. 个人资料和设置页接入真实接口。
5. 完整异常态、空状态和埋点。

## 4. 架构与服务归属

- `Gateway`：承接所有业务 HTTP 路由；新增 `/api/session/**`、`/api/notification/**` 和新的群管理路由。
- `UserService`：用户资料、好友、群、会话摘要、会话设置、成员权限和已读位置。
- `RealTimeService`：WebSocket 鉴权、在线路由、心跳、消息接收状态和实时推送。
- `OfflineDataService`：消息持久化、历史消息、离线同步、消息状态查询和持久化结果事件。
- `RedPacketService`：红包账务、领取、退款、过期和红包详情。
- `MinIO`：图片原文件和缩略图；业务数据库只保存稳定对象标识和元数据。
- 不因为新增 URL 单独创建新的微服务。

## 5. WebSocket 鉴权需求

### 5.1 移动端主方案

- 正式 App 通过原生 WebSocket 客户端建立连接。
- 握手使用 `Authorization: Bearer <accessToken>`。
- 服务端统一支持 Bearer 格式，不再把整个 Header 原样当 token。
- 服务端从 JWT 确定当前用户，不信任消息体里的 `senderId`。
- 连接建立时校验 token、Redis 登录态和用户状态。
- 生产环境必须使用 `wss://`。

### 5.2 浏览器兼容方案

- 浏览器不是下一轮正式发布目标，但需要保留联调能力。
- 使用 `POST /api/user/ws-ticket`，通过正常 HTTP 鉴权换取一次性短期 ticket。
- ticket 有效期不超过 60 秒，只允许使用一次，并绑定用户和目标 WebSocket 服务。
- 浏览器只在握手时携带短期 ticket，不得把 accessToken 或 refreshToken 放入 URL。
- ticket 获取失败时，浏览器根据登录失效或网络异常分别进入鉴权失败和重连状态。

### 5.3 地址刷新

- 登录成功返回当前可用的 `nettyUri`。
- token 刷新或连接连续失败时，客户端调用 `GET /api/user/refresh/uri` 获取新地址。
- `nettyUri` 必须是客户端真实可达地址，不能返回容器内部主机名或固定 localhost。
- 后台恢复、路由 TTL 过期、服务实例下线时重新握手。

## 6. 消息 ACK 与幂等

### 6.1 ACK 阶段

服务端通过 WebSocket 返回统一事件：

```json
{
  "event": "message-ack",
  "data": {
    "clientMessageId": "client-uuid",
    "messageId": "1900000000001",
    "sessionId": "501",
    "stage": "accepted",
    "createdTime": 1760000000000,
    "errorCode": null,
    "errorMessage": null
  }
}
```

- `accepted`：RealTimeService 已校验并接收消息。
- `persisted`：OfflineDataService 已可靠落库。
- `failed`：消息明确失败，必须返回稳定错误码。
- `accepted` 只用于内部状态，不直接向用户显示“已发送”。
- 收到 `persisted` 后，前端将消息标记为“已发送”。
- 超时且没有明确失败时标记为“结果待确认”。

### 6.2 幂等要求

- 客户端生成全局唯一 `clientMessageId`。
- 服务端以“已认证发送者 + clientMessageId”建立唯一约束。
- 同一个发送者使用相同 `clientMessageId` 重试时，返回原来的 `messageId` 和最终结果。
- 服务端不得为同一次重试创建第二条消息。
- 图片和红包同样遵循该幂等规则。
- WebSocket 消息中的 `senderId` 可以保留兼容，但服务端必须覆盖为已鉴权用户。
- 服务端必须校验发送者是否属于对应单聊或群聊会话。

### 6.3 消息结果查询

新增：

- `GET /api/message/status?clientMessageId=`：查询当前账号某条待确认消息的最终状态。
- 可选增加批量接口，供 App 重启后查询多条待确认消息。

返回至少包含：

- `clientMessageId`；
- `messageId`；
- `sessionId`；
- `status`：accepted、persisted、failed、notFound；
- `createdTime`；
- 稳定错误码和可展示错误信息。

## 7. 离线同步协议

### 7.1 新接口形式

离线接口已升级为游标分页：

- `POST /api/message/offline/sync`
- 请求：`cursor`、`limit`，首次同步 cursor 可空。
- 当前用户从 JWT 获取，请求体不再传 `userId`。

返回：

```json
{
  "items": [],
  "nextCursor": "opaque-server-cursor",
  "hasMore": false,
  "serverTime": 1760000000000
}
```

### 7.2 游标规则

- 游标由服务端生成，客户端不得解析或自行拼接。
- 游标内部至少能稳定表达 `createdTime + messageId` 的顺序。
- 同一毫秒有多条消息时不能遗漏。
- 客户端只有在整页成功合并并持久化后才保存 `nextCursor`。
- 同一页重复请求必须返回可安全去重的数据。
- 客户端按 `messageId` 去重，待发送消息再按 `clientMessageId` 归并。

### 7.3 数据完整性

- Redis 热数据缺失时必须回源 MySQL。
- 不能只依赖近 7 天 Redis 数据承诺完整补拉。
- WebSocket 建连后再启动补拉，实时消息和补拉消息允许重叠。
- 补拉期间收到的实时消息必须保留，最终统一排序和去重。
- 系统通知不混入普通聊天消息；重连时单独刷新好友申请和通知列表。

## 8. 会话摘要、详情与未读

### 8.1 会话摘要接口

新增 `GET /api/session/list?cursor=&limit=20`。

每项至少返回：

- `sessionId`；
- `sessionType`；
- `name`；
- `avatar` 或稳定头像对象 ID；
- `peerId`，仅单聊返回；
- `lastMessage`；
- `lastMessageId`；
- `lastMessageTime`；
- `unreadCount`；
- `pinned`；
- `muted`；
- `memberCount`，群聊返回；
- `currentUserRole`，群聊返回；
- `version` 或 `updatedTime`。

列表要求：

- 置顶会话在前，置顶区内按最后消息时间排序。
- 普通会话按最后消息时间倒序。
- 分页期间有新消息时不能永久跳过会话。
- 被隐藏的会话收到新消息后重新出现。

### 8.2 会话详情

新增 `GET /api/session/{sessionId}`，返回：

- 会话基本资料；
- 当前用户是否仍在会话内；
- 单聊对方资料或群资料；
- 当前用户权限；
- 置顶和免打扰状态；
- 最后已读位置。

### 8.3 已读与未读

- `POST /api/session/{sessionId}/read`。
- 请求携带 `lastReadMessageId`；服务端记录当前用户已读位置。
- `GET /api/message/unread` 返回各会话服务端未读数。
- 进入会话且消息可见后提交已读，不以打开页面瞬间直接清零。
- 重连和冷启动后以后端未读数为准。
- 当前会话收到新消息时不增加未读；离开会话后再按服务端结果校正。

## 9. 群资料与群管理

### 9.1 群详情和成员

- `GET /api/session/{sessionId}` 返回群名称、头像、公告、成员数和当前用户角色。
- `GET /api/group/{sessionId}/members?cursor=&limit=50` 分页返回成员。
- 成员字段包含 userId、昵称、群昵称、头像、角色、加入时间和状态。
- 成员排序：群主、管理员、普通成员；同角色保持稳定排序。

### 9.2 群资料修改

- `PATCH /api/group/{sessionId}`：修改群名称和公告。
- `GET /api/group/{sessionId}/avatar/upload-url`：获取群头像上传地址。
- `PUT /api/group/{sessionId}/avatar`：保存对象 ID 和头像元数据。
- 修改成功后通过系统事件通知其他在线成员刷新群资料。

### 9.3 成员操作

- `POST /api/group/invite`：沿用现有邀请接口和请求体，并统一返回成功和失败成员；本轮不为调整 URL 制造破坏性变更。
- `DELETE /api/group/{sessionId}/members/{userId}`：移除成员。
- `POST /api/group/{sessionId}/leave`：普通成员退出。
- `DELETE /api/group/{sessionId}`：群主解散群聊。
- `POST /api/group/{sessionId}/transfer-owner`：转让群主。
- `POST /api/group/{sessionId}/admins`：设置管理员。
- `DELETE /api/group/{sessionId}/admins/{userId}`：取消管理员。

### 9.4 权限要求

- 群主可以修改群资料、管理管理员、移除成员、转让和解散。
- 管理员可以按规则修改公告和移除普通成员。
- 普通成员可以查看资料、邀请好友和退出群聊。
- 所有权限由服务端校验，前端隐藏按钮只用于交互，不作为安全措施。
- 每项失败必须返回明确错误码，例如无权限、成员不存在、群已解散或不能移除群主。

## 10. 图片和媒体资源

### 10.1 数据保存

- 数据库保存 `objectName`、媒体类型、宽高、大小和可选缩略图对象 ID。
- 不把预签名上传 URL 写入数据库。
- 不把会过期的下载 URL 当作消息永久正文。
- 消息体优先保存稳定 `mediaId` 或 `objectName`。

### 10.2 访问方式

- 公开资源可以由稳定 CDN 地址访问。
- 私有资源通过 `GET /api/file/{objectName}/download-url` 获取短期下载地址。
- 下载地址过期时前端自动刷新一次，不把它视为消息发送失败。
- 会话列表使用缩略图或 `[图片]` 摘要，不下载原图。
- 图片查看页支持加载、失败、重新获取地址和保存权限提示。

### 10.3 上传状态

- 上传对象成功但消息发送失败时保留本地上传结果。
- 用户重试消息时复用已有 `objectName`，不重复上传同一文件。
- 取消上传应停止网络请求，并清理没有被消息引用的临时对象。
- 服务端需要有临时对象清理策略。

## 11. 本地数据与安全存储

### 11.1 正式移动端

- accessToken、refreshToken、当前账号和环境标识写入 Keychain 或 Android Keystore。
- SQLite 或等价本地数据库保存会话、消息、草稿、未读、同步游标和待确认消息。
- 不保存明文密码、验证码、WebSocket Header 和预签名上传 URL。
- 退出登录时清除令牌、WebSocket 连接和当前账号的敏感缓存。
- 切换账号时严格按 userId 隔离数据库记录和同步游标。

### 11.2 浏览器原型

- `localStorage` 只用于演示和开发联调。
- 页面和文档中明确浏览器缓存不等同于正式安全存储。
- 不在日志、错误提示或埋点中输出 token、ticket 和预签名 URL。

## 12. 消息重试和聊天交互

### 12.1 状态定义

- `uploading`：媒体正在上传。
- `sending`：已经提交，等待 ACK。
- `sent`：收到 persisted ACK。
- `unknown`：超时，没有明确成功或失败。
- `failed`：收到明确失败结果。

### 12.2 重试规则

- 当前客户端不自动重发，避免在网络抖动时产生意外操作。
- 用户可以点击 unknown 或 failed 消息查看结果并手动重试。
- 重试必须复用原 `clientMessageId`。
- 重试前先调用消息状态查询；已持久化则直接归并，不再次发送。
- 明确不可重试的错误直接提示原因，例如被移出群聊或会话不存在。
- 网络类错误允许重试，权限类和参数类错误不提供无意义重试。

### 12.3 聊天页补充体验

- 历史消息继续向上分页，保持当前阅读位置。
- 用户停留在旧消息区域时，新消息不强制滚到底部。
- 显示“有新消息”浮层，点击后滚到底部。
- 长按消息第一阶段支持复制和删除本地记录；撤回、回复和转发按后端接口逐步开放。
- 空消息、超长文本和重复点击发送需要前端限制。

## 13. 红包主流程

### 13.1 发送

- 支持普通红包和拼手气红包。
- 输入金额、数量和祝福语；单聊普通红包数量固定为 1。
- 发送使用 `POST /api/chat/redPacket/send`。
- 当前用户身份从 JWT 获取，请求体不再信任 `senderId`。
- 请求携带 `clientMessageId`，服务端按当前用户和该字段幂等。
- HTTP 成功后立即关联本地红包气泡，再与 WebSocket 和历史消息合并。
- 发送超时先查询结果，不自动再次扣款。

### 13.2 领取和详情

- `POST /api/chat/redPacket/receive`：当前用户从 JWT 获取。
- `GET /api/chat/redPacket/basic?redPacketId=`：红包气泡状态。
- `GET /api/chat/redPacket/?redPacketId=&pageNum=&pageSize=`：红包详情和领取记录。
- 页面覆盖可领取、已领取、已领完、已过期和领取失败。
- 同一次领取请求重复到达不得重复入账。
- 金额统一用十进制字符串传输和展示，避免浮点误差。

### 13.3 红包验收底线

- 网络重试不能重复扣款或重复领取。
- 红包过期后退款可恢复，Kafka 失败不能永久丢失过期任务。
- 前端只展示服务端最终账务状态，不自行推算余额。

## 14. 会话与联系人管理

- `POST /api/session/{sessionId}/pin`：置顶或取消置顶。
- `POST /api/session/{sessionId}/mute`：开启或关闭免打扰。
- `DELETE /api/session/{sessionId}`：隐藏会话，不删除对方数据。
- `PATCH /api/contact/friend/{friendId}/remark`：修改好友备注。
- 联系人和会话名称展示优先级：好友备注、群名称、用户昵称、兜底名称。
- 操作采用乐观更新时必须支持失败回滚和提示。
- 新消息到达被隐藏会话后，会话重新出现在列表中。

## 15. 个人资料与设置

- `GET /api/user/me`：读取当前用户资料。
- `PATCH /api/user/me`：修改昵称、性别和签名。
- 头像继续走预签名上传，但数据库保存稳定对象标识。
- 设置页至少包含账号信息、通知设置、缓存说明、隐私入口和退出登录。
- 发现页下一轮只保留稳定信息架构，不扩展内容推荐业务。

## 16. 错误码与用户提示

后端需要为公开接口补充稳定错误码，至少覆盖：

- token 失效或账号被下线；
- WebSocket ticket 无效或已使用；
- 不属于会话；
- 群已解散；
- 被移出群聊；
- 没有群管理权限；
- 消息重复且已成功；
- 消息持久化失败；
- 同步游标无效或过期；
- 上传对象不存在；
- 红包已领、已领完、已过期或余额不足。

前端提示原则：

- 能恢复的网络问题使用页面内轻提示，不连续弹窗。
- 权限和业务失败展示明确原因。
- `unknown` 不显示为明确失败。
- 认证失效停止重连，清理登录态并回到登录页。
- 所有失败页面提供返回、刷新或重试路径。

## 17. 验收场景

### 17.1 实时消息

1. 两个真实账号连接不同 RealTimeService 实例。
2. A 给 B 发送文本和图片。
3. A 收到 accepted 和 persisted ACK，最终只保留一条消息。
4. B 实时收到消息，单聊和群聊路由正确。
5. 重复发送同一 `clientMessageId`，数据库和双方页面仍只有一条。
6. 非会话成员伪造 sessionId 发送时被服务端拒绝。

### 17.2 断线与离线恢复

1. B 断网，A 连续发送多条消息。
2. B 恢复网络后先连接 WebSocket，再分页补拉。
3. 补拉期间 A 继续发送消息，B 不漏消息也不重复。
4. 同一毫秒内产生超过一页消息，联合游标仍可完整翻页。
5. Redis 热数据不存在时通过 MySQL 回源恢复。
6. App 杀掉重启后从已保存的服务端游标继续同步。

### 17.3 会话与未读

1. 新账号登录后能从服务端恢复会话列表。
2. 最后消息、时间、未读、置顶和免打扰跨设备一致。
3. 进入会话并看到消息后提交已读。
4. 重连后未读数与服务端一致。
5. 隐藏会话收到新消息后重新出现。

### 17.4 群管理

1. 群主修改名称、公告和头像。
2. 管理员只能执行允许的操作。
3. 普通成员不能调用管理接口。
4. 退出、移除和解散后，会话与发送权限立即更新。
5. 成员列表分页无重复、无漏项。

### 17.5 红包

1. 单聊和群聊可以发送红包。
2. 重复提交不重复扣款。
3. 多人并发领取不超发。
4. 已领完、已过期和重复领取提示正确。
5. 过期退款在服务重启和消息重试后仍能完成。

### 17.6 安全与稳定性

1. 客户端不能伪造 senderId、userId 或群角色。
2. 日志和埋点不包含 token、ticket 和预签名 URL。
3. accessToken 过期后停止无效重连并完成刷新或退出。
4. 网络切换和 App 前后台切换不会创建并存的重复连接。
5. 所有接口经过 Gateway，WebSocket 和对象存储使用明确的外部可达地址。

## 18. 测试要求

- 前端继续使用 Playwright 覆盖浏览器原型的核心页面和异常状态。
- 移动端必须增加真机或模拟器测试，浏览器原型不能替代 WebSocket Header、安全存储和后台恢复测试。
- 后端增加消息幂等、ACK、离线游标、未读数和群权限的集成测试。
- 增加两个 RealTimeService 实例的跨实例消息测试。
- 增加 Redis 热数据缺失后的 MySQL 回源测试。
- 增加 Kafka 重复消费、重试和死信测试。
- 红包增加并发领取、重复请求、过期退款和服务重启恢复测试。

## 19. 交付物

### 后端

- 已评审的接口文档和错误码表。
- WebSocket 鉴权与 ACK 协议说明。
- 会话、未读和离线游标接口。
- 群详情与基础管理接口。
- 消息和红包幂等约束及迁移脚本。
- Gateway 路由和环境配置示例。
- 集成测试结果。

### 前端

- 协议适配层和本地数据模型说明。
- 会话列表、聊天、群资料、红包和设置页面的完整状态。
- 待确认消息查询和手动重试流程。
- 安全存储和账号隔离方案。
- Playwright 与真机测试记录。

## 20. 开发前必须定稿的事项

1. WebSocket 鉴权已定稿：移动端使用 Bearer Header，浏览器同步提供一次性短期 ticket，生产环境统一使用 WSS。
2. `message-ack` 已定稿为 accepted、persisted、failed 三阶段，前端 8 秒未获最终结果时查询状态。
3. `clientMessageId` 已按发送者建立数据库唯一约束，重复请求返回原 messageId 和最终状态。
4. 离线游标已定稿：服务端签名的 v1 不透明游标，绑定用户并表达 `createdTime + messageId`；默认每页 20 条、最多 100 条，MySQL 为完整数据源，当前不设置游标保留期限。
5. 会话摘要由 UserService 实时聚合还是通过事件维护读模型。
6. 未读位置使用 `lastReadMessageId` 还是服务端联合游标。
7. 图片使用公开 CDN 还是私有临时下载地址。
8. 群管理员权限边界和群解散后的数据保留规则。
9. 红包金额字段的传输格式、幂等键和结果查询接口。
10. 正式移动端框架、安全存储库和本地数据库选型。

## 21. 本轮完成标准

- 真机可以稳定登录、连接、重连和恢复会话。
- 文本和图片消息有明确的持久化确认。
- 相同 `clientMessageId` 重试不会产生重复消息。
- App 冷启动后能从服务端恢复会话、未读和离线消息。
- 群资料和成员权限可以从服务端完整恢复。
- 图片消息不依赖永久有效的预签名 URL。
- 红包发送、领取、详情和异常账务状态可完整展示。
- 令牌存放在系统安全存储，账号数据严格隔离。
- 关键异常场景有明确反馈并可恢复。
- 多实例、断线、重复请求和服务重启场景通过验收。
